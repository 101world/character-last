# Core ML & DL
torch==2.6.0
torchvision==0.21.0
transformers>=4.40.0
accelerate>=0.29.3
safetensors>=0.4.3
huggingface-hub>=0.22.2
datasets>=2.19.1
bitsandbytes>=0.43.0

# Image processing
Pillow>=9.5.0
opencv-python

# Captioning & text encoders (optional for dataset prep)
open_clip_torch>=2.24.0
sentence-transformers>=2.2.2
ftfy
regex

# Cloudflare R2 integration
boto3>=1.28.0
botocore>=1.31.0

# Utilities
runpod
tqdm
einops>=0.7.0
omegaconf>=2.3.0
toml>=0.10.0